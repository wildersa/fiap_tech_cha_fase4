{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "pip install poetry==1.8.2 && poetry config virtualenvs.create false && poetry install --only main --no-interaction --no-ansi",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}
